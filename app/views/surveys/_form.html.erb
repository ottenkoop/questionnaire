<%= form_for @survey do |s| %>

	<%= s.label :name, "Category for the Survey" %>
	<%= s.text_field :name %><br />

		<%= s.fields_for :questions do |builder| %>
		<%= render 'question_fields', f: builder %>
		<% end %><br/>

	<%= s.submit "Submit" %>

<% end %>