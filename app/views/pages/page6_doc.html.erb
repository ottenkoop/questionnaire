<% if @result.answers.sum(:points).to_f / @result.answers.count.to_f >= 0.60 && @result.answers.first.question.survey.name == "Doctor" %>

	<p><% session[:dream1] = true %> Goed gedaan Doctor </p>

<% elsif @result.answers.sum(:points).to_f / @result.answers.count.to_f >= 0.60 && @result.answers.first.question.survey.name == "Teacher" %>

<div class="container-fluid page1_bg">


		<div class="row-fluid">
          <div class="span12 topmenu_bg">

              <div class="">
                <a href="#" class="home_btn"></a>
              </div>

              <div class="">
                <a href="#" class="help_btn"></a>
              </div>

          </div>
      	</div>

		<div class="row-fluid">
			<div class="span12  visible-phone">
				<img src="assets/bobo_good_answer_mob.png" class="result_bobo_bad"/>
			</div>
		</div>

		<div class="row-fluid">
			<div class="span12  visible-phone">
				<div class="result_bubble">
					<p class="top"> GOOD JOB!</p>
					<p class="bottom"> YOU HAVE UNLOCKED LEVEL 3! </p>
				</div>
			</div>
		</div>

		<div class="span12 result_play_again visible-phone">
			<a href="/3"><h5>PICK A NEW DREAM</h5></a>
		</div>


	</div>

<% else %>

	<div class="container-fluid result_bg">


		<div class="row-fluid">
          <div class="span12 topmenu_bg">

              <div class="">
                <a href="#" class="home_btn"></a>
              </div>

              <div class="">
                <a href="#" class="help_btn"></a>
              </div>

          </div>
      	</div>

		<div class="row-fluid">
			<div class="span12  visible-phone">
				<img src="assets/bobo_wrong_answer_mob.png" class="result_bobo_bad"/>
			</div>
		</div>

		<div class="row-fluid">
			<div class="span12  visible-phone">
				<div class="result_bubble">
					<p class="top"> YOU CAN DO BETTER! </p>
					<p> TO GET TO THE NEXT LEVEL, YOU HAVE </p>
					<p class="bottom"> TO DO THIS LEVEL AGAIN. </p>
				</div>
			</div>
		</div>

		<div class="span12 result_play_again visible-phone">
			<a href="#"><h5>Play again</h5></a>
		</div>


	</div>


<% end %>




<%= debug @result.answers.first.question.survey.name == "Teacher" %>
<%= @result.answers.first.content %>

<%= debug @result.answers.count.to_f %> 

<%= debug @result.answers.sum(:points).to_f + 2 %>

<%= debug @result.answers.sum(:points).to_f / @result.answers.count.to_f >= 0.60 %>

    <div class="row-fluid">
      <div class="span3 offset9 button ss-button">
        <a href="/3"><img src="../assets/next_button.png" alt="Next" width="104" height="41"></a>
      </div>
    </div>
  </div>